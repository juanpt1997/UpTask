extends layout

//- El mixin sirve para tener una clase que contiene métodos para otras clases
include mixins/crearProyecto


block contenido
    .contenido-principal
        h1 #{nombrePagina}

        //- pre #{vardump(proyecto)}
        
        +crearProyecto(proyecto, errores)